# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: Organization ID (set via UI if not provided)
NEXT_PUBLIC_ORG_ID=

# Optional: Organization slug for multi-tenant setup
NEXT_PUBLIC_ORG_SLUG=
